/*
Parallel and Sequential Promise Executor
Build a class named PromiseExecutor with two methods: executeParallel and executeSequential, each accepting an array of asynchronous functions. Execute the first method in parallel,
and the second sequentially, measuring and logging execution times.
 */
